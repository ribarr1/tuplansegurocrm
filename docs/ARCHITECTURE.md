# Arquitectura

Documento breve. Describe el flujo de una operación y qué capa hace qué — no es un manual exhaustivo.

## Flujo de una operación

```
UI (Client Component: formulario)
  ↓  <form action={...}> (useActionState)
Server Action                        [src/app/(app)/contacts/actions.ts]
  ↓
requireSessionUser() / requireSessionRole(...)   [src/lib/authorization.ts]
  ↓  produce un "actor" (AuthorizedUser) ya verificado
Service                              [src/services/*.service.ts]
  ↓  valida input (Zod) + aplica reglas de negocio/permisos sobre ese actor
Prisma                               [src/lib/prisma.ts]
  ↓
PostgreSQL
```

Para páginas/Server Components que solo leen datos (sin mutación), se llama al servicio **directamente desde el Server Component** — no hace falta una Server Action para una lectura en el render inicial. Ejemplo: `src/app/(app)/contacts/page.tsx` llama `listPeople(actor, ...)` directo. Las Server Actions existen específicamente para mutaciones disparadas desde un Client Component (formularios) — ver `src/app/(app)/contacts/actions.ts`.

Para páginas/Server Components que solo necesitan proteger una ruta sin llamar un servicio, se usa `requireUser()`/`requireRole()` directamente — ver `src/app/(app)/dashboard/page.tsx`. Toda el área protegida comparte un layout común: `src/app/(app)/layout.tsx` llama `requireUser()` una vez y renderiza el shell (sidebar + header); `src/proxy.ts` solo hace una redirección optimista (existencia de cookie) antes de eso.

## Por qué la autorización está partida en dos

1. **Identidad** (¿quién eres, y estás activo?) se resuelve **una sola vez**, justo antes de invocar el servicio, con `requireSessionUser()`/`requireSessionRole()`. Producen un `AuthorizedUser` ya verificado contra Prisma en ese instante — nunca se construye a mano a partir de datos del cliente.
2. **Permiso sobre la operación concreta** (ej. "¿puede este AGENT editar esta Person en particular?") vive **dentro del servicio**, usando el `actor` recibido. Cada función de servicio exige `actor: AuthorizedUser` como primer parámetro obligatorio.

Esto evita que una regla de negocio sensible se salte por reutilizar un servicio desde un lugar nuevo que "se olvidó" de comprobar el rol antes de llamarlo — la regla vive en el propio servicio, no en el llamador.

## Qué NO debe importar Prisma directamente

- **Client Components** (`"use client"`) — nunca. `src/lib/prisma.ts`, `src/services/*` y `src/lib/authorization.ts` usan `import "server-only"` precisamente para que un import accidental desde un Client Component falle en build, no en producción.
- **Páginas/Route Handlers** no deberían llamar `prisma.*` directamente para operaciones de negocio — deben pasar por un servicio. Se acepta Prisma directo únicamente para lecturas triviales de infraestructura (ninguna todavía en el proyecto).

## Errores de aplicación

`src/services/errors.ts` — `AppError` con `code` (`UNAUTHORIZED` | `FORBIDDEN` | `NOT_FOUND` | `VALIDATION_ERROR` | `CONFLICT`) y `statusCode` ya resuelto. Los servicios lanzan `AppError` para casos previsibles; nunca dejan escapar errores crudos de Prisma (SQL, nombres de constraint, stack traces) hacia el usuario.

## Validación

Zod, en `src/schemas/*.schema.ts`. Los servicios validan el input **antes** de tocar Prisma, usando `parseOrThrow()` (`src/services/errors.ts`), que traduce un error de Zod a `AppError("VALIDATION_ERROR", ...)`. No se duplican validaciones que PostgreSQL ya garantiza (unicidad, FKs, CHECK).

## Transacciones

`createPerson`/`updatePerson` (single-entity) no usan `$transaction` — no hay necesidad real. Cuando una operación futura deba crear/modificar múltiples entidades de forma atómica (ejemplo: `Household` + `HouseholdMember`s + `Policy` en un solo flujo), usar `prisma.$transaction([...])` (forma de array, para operaciones independientes entre sí) o `prisma.$transaction(async (tx) => {...})` (forma interactiva, cuando una operación depende del resultado de la anterior dentro de la misma transacción).

## Logging

Sin sistema de logging todavía. Regla mientras tanto: nunca loggear password, `BETTER_AUTH_SECRET`, contenido de `PersonMedication`/`PersonProvider.notes`, montos/datos financieros sensibles, ni objetos Prisma completos. Errores técnicos pueden loggearse server-side de forma sanitizada (mensaje + código, no el objeto de error crudo de Prisma).

## Multi-tenancy

No implementado ni planeado. TuPlanSeguro USA es una sola agencia — no hay `tenantId` en ninguna tabla, y no se anticipa esa necesidad.

## Tests

Vitest, contra el PostgreSQL local real (mismo patrón usado en cada migración: sin mocks de Prisma). `server-only` se alias a un no-op en `vitest.config.mts` porque ese paquete lanza intencionalmente fuera del compilador de Next.js. Los tests de servicio construyen el `actor` directamente (sin pasar por sesión real); un test separado (`src/lib/authorization.test.ts`) prueba la resolución de sesión real usando `auth.api.signInEmail`.

Los flujos que dependen del servidor Next.js real corriendo (protección de rutas por proxy/layout, formularios completos con Server Action, permisos reflejados en la UI) se verifican con el servidor de desarrollo levantado (`npm run dev`) — mismo patrón manual usado en la Fase 007 para Auth, no Playwright (no instalado, sin necesidad real todavía).

## CRM Shell + Contactos — Fase 009

- **Shell:** `src/app/(app)/layout.tsx` (protegido) + `src/components/shell/*` (`Sidebar`, `Header`, `MobileNav`, `UserMenu`, `NavContent`). Navegación centralizada en `src/components/shell/nav-items.ts` — un módulo sin implementar se marca `enabled: false` ahí y aparece deshabilitado en la UI, nunca como una página que finja funcionar.
- **UI library:** [shadcn/ui](https://ui.shadcn.com), preset `base-nova`, construido sobre **Base UI** (no Radix). Composición polimórfica usa `render={<Elemento />}` en vez de `asChild` + hijo — ej. `<Button render={<Link href="..." />}>Texto</Button>`. Cuando el elemento renderizado no es un `<button>` real (como `Link`), se agrega `nativeButton={false}` — de lo contrario Base UI emite un warning de accesibilidad en consola.
- **Contactos es el primer (y único) módulo funcional.** `listPeople`/`getPersonById`/`createPerson`/`updatePerson` (Fase 008) alimentan `/contacts`, `/contacts/[id]`, `/contacts/new`, `/contacts/[id]/edit`. Sin `deletePerson` — no hay eliminación física de contactos en el CRM.
- **Búsqueda/filtro/paginación viven en la URL** (`?q=&status=&page=`), no en estado de cliente — el formulario de filtro es un `<form method="GET">` nativo, sin JavaScript necesario para funcionar.
- **`assignedAgentId` en los formularios:** el `<select>` de agente solo se renderiza para ADMIN (`showAgentSelect = actor.role === "ADMIN"`) — AGENT/ASSISTANT ni lo ven, para no sugerir un control que la política del servicio va a ignorar de todas formas. La UI es conveniencia, nunca la autoridad: `people.service.ts` aplica la política igual si alguien arma el POST a mano.
- **`canEditPerson` (people.service.ts) es la misma función que decide si `/contacts/[id]/edit` muestra el formulario o "no tienes permiso".** No hay una segunda copia de la regla en la UI — si se relaja o endurece la política de edición, cambia en un solo lugar.
- **Errores de formulario en español, nunca el texto crudo de Zod.** Los `.min()`/`.email()`/etc. en `src/schemas/person.schema.ts` llevan mensaje explícito (ej. `"El teléfono debe tener al menos 7 caracteres."`) — sin esto, el usuario vería el mensaje interno en inglés de Zod tal cual.
- **`ContactForm` repite los valores enviados tras un error** (`state.values`, `src/app/(app)/contacts/form-helpers.ts`) y usa un `key` en el `<form>` para forzar remount. React 19 limpia los campos no controlados de un formulario al terminar una Server Action — sin este mecanismo, un error de validación borraría lo que el usuario ya había escrito.
- **`formDataToPersonInput`/`toFormState` viven en `form-helpers.ts`, no en `actions.ts`.** Un archivo `"use server"` solo puede exportar funciones `async` — separar la lógica pura permite probarla con Vitest sin esa restricción.

## Household / Familia — Fase 010

- **Vive dentro del perfil de contacto, no como módulo propio.** El tab "Familia" de `/contacts/[id]` (`src/app/(app)/contacts/[id]/family-tab.tsx`) es la única superficie de UI — no hay una ruta `/households` independiente en esta fase.
- **`households.service.ts`** expone `getHouseholdsForPerson`, `getHouseholdById`, `createHouseholdWithInitialMember`, `addHouseholdMember`, `removeHouseholdMember`, `updateHouseholdMemberRole`, `createPersonAndAddToHousehold` — mismo patrón que `people.service.ts`: `actor: AuthorizedUser` como primer parámetro obligatorio, `select` explícito sin datos médicos/financieros, `AppError` para todo caso previsible (`NOT_FOUND`, `FORBIDDEN`, `CONFLICT`, `VALIDATION_ERROR`).
- **`createHouseholdWithInitialMember` y `createPersonAndAddToHousehold` usan `prisma.$transaction(async (tx) => {...})`** — primer caso real en el proyecto de la forma interactiva mencionada en la sección "Transacciones" arriba (cada paso depende del anterior: crear el hogar/persona y enlazar la membresía deben ocurrir juntos o no ocurrir).
- **Server Actions** en `src/app/(app)/contacts/household-actions.ts` — mismo patrón que `contacts/actions.ts`: llaman `requireSessionUser()`, delegan toda regla de negocio al servicio, traducen `AppError` a `HouseholdFormState` (`household-form-helpers.ts`), y hacen `revalidatePath('/contacts/[id]')` tras cada mutación. `searchPeopleAction` reutiliza `listPeople` (Fase 008) — no hay un segundo servicio de búsqueda de personas.
- **La UI de un hogar se renderiza de forma independiente en el perfil de cada uno de sus miembros.** No existe una vista "canónica" única de `Household` — al abrir el tab Familia de cualquier persona del hogar se consulta y renderiza esa misma entidad vía `getHouseholdsForPerson`. Consecuencia observada en pruebas manuales: una mutación hecha desde el perfil de la Persona A revalida solo `/contacts/[A]`; el perfil de la Persona B (mismo hogar) muestra los datos actualizados en su próxima carga de servidor (no requiere acción adicional, pero no es una revalidación "en vivo" cross-perfil).

## Pólizas — Fase 011

- **`policies.service.ts`** expone `listPolicies`, `getPolicyById`, `getPoliciesForPerson`, `createPolicy`, `updatePolicy` (sin `deletePolicy`), más `listActiveCarriers`/`listActiveProducts` como catálogo de solo lectura — mismo patrón que el resto de servicios (`actor` primero, `select` explícito sin datos de salud/comisiones, `AppError` para todo caso previsible).
- **`createPolicy` es una transacción** (`Policy` + `PolicyMember`(s)): si cualquier miembro falla (ej. FK inválida), toda la póliza se revierte — nunca queda una `Policy` sin sus miembros o a medias.
- **Autorización de Policy reutiliza el mismo criterio que `Household` (Fase 010), aplicado a "titular + miembros cubiertos" en vez de "miembros del hogar".** ADMIN/ASSISTANT sin restricción; AGENT necesita acceso (sin asignar o asignado a sí mismo) a AL MENOS UNA persona involucrada (titular o algún miembro cubierto). `listPolicies`/`getPoliciesForPerson` aplican ese mismo filtro a nivel de `where`, no solo como guard post-consulta — un AGENT nunca ve en la lista una póliza fuera de su acceso.
- **El navegador nunca decide `policyType`/`carrier` de una póliza.** El formulario los usa únicamente para filtrar qué `Product` mostrar en el `<select>`; solo `productId` viaja al servidor, que vuelve a consultar `Product → Carrier` — igual que la regla ya establecida para `Policy.product` en el schema.
- **`/policies/new` usa la URL como estado, igual que `/contacts` y `/contacts/[id]/edit`:** el titular (`?holderId=`), el filtro de tipo/compañía (`?policyType=&carrierId=`) y la búsqueda de titular (`?holderSearch=`) son query params — cambiar de titular o filtrar productos es una navegación (`GET`), no estado de cliente. Solo el envío final (crear la póliza) es un `POST`/Server Action.
- **Los candidatos a "miembro cubierto" en el formulario de creación provienen de los hogares del titular** (`getHouseholdsForPerson`), no de una búsqueda global de personas — decisión explícita de mantener el primer flujo simple (ver docs/DECISIONS.md). El servicio, sin embargo, no restringe `coveredMembers` a esos candidatos: valida que cada `personId` exista, igual que cualquier otro input — la lista de candidatos es una conveniencia de UI, no el límite de seguridad real (mismo principio que `showAgentSelect` en Fase 009).
- **`premiumAmount` viaja como string validado por regex (`policy.schema.ts`) en todo el camino UI → Server Action → servicio → Prisma**, nunca como `number` de JavaScript — Prisma acepta un string para un campo `Decimal`, evitando aritmética de punto flotante sobre un monto financiero.
- **La regla "ACTIVE requiere `effectiveDate`" se evalúa dos veces con la misma función (`assertActiveHasEffectiveDate`): en `createPolicy` sobre el input completo, y en `updatePolicy` sobre el estado ya combinado (existente + lo que se está actualizando)** — evita que una edición parcial (ej. solo cambiar `premiumAmount`) deje una póliza `ACTIVE` sin fecha efectiva por accidente.
- **Bug real encontrado en pruebas manuales, corregido antes de cerrar la fase:** en el formulario de edición, tras un error de validación, los checkboxes `autopay`/`needsPaymentAssistance` se re-renderizaban siempre marcados — `state.values` los devuelve como texto ("false") para poder repetirlos en el formulario, y una string no vacía es *truthy* en JavaScript, así que un simple `{...defaultValues, ...state.values}` los volvía todos `true` visualmente aunque el valor real fuera "false". Se corrigió resolviendo esos dos campos aparte con una comparación explícita (`=== "true"`) antes de construir `values`.
- **Gestión de miembros de una póliza ya existente (Fase 019.7, Hallazgo #12):** `policies.service.ts` gana `getPolicyMembersDetailed`, `getEligibleHouseholdMembersForPolicy`, `addPolicyMember`, `removePolicyMember`. "Elegible" se deriva en tiempo real de `Policy.householdId` menos las personas que ya tienen fila `PolicyMember` — nunca una lista cacheada, así que un miembro agregado al hogar después de crear la póliza aparece disponible de inmediato sin ningún paso adicional. `removePolicyMember` borra únicamente la fila `PolicyMember`, nunca toca `Person`/`HouseholdMember`. Ningún flujo de agregar a un `Household` dispara automáticamente una cobertura de `Policy` (NO AUTO-ENROLL, verificado en test B).
- **Reparar `Policy.householdId` cuando quedó `null` (Fase 019.8, Hallazgo #17):** `getHouseholdLinkCandidates`/`linkPolicyToHousehold` cubren el caso en que el Household se crea DESPUÉS de la póliza, o el titular tenía 2+ hogares al crearla (ambos dejan `householdId=null` por diseño). `linkPolicyToHousehold` valida que el `householdId` dado sea realmente uno de los hogares del titular (nunca uno arbitrario) y rechaza (`CONFLICT`) si la póliza ya está vinculada — todavía no existe una acción de "cambiar de hogar".
- **`USDateInput` (Fase 019.8, Hallazgo #16):** `src/components/ui/us-date-input.tsx` reemplaza `<input type="date">` en los 4 campos date-only donde el usuario introduce una fecha (DOB, `effectiveDate`/`terminationDate`/`nextPaymentDueDate`). Un campo de texto enmascarado (nunca enviado) más un `<input type="hidden">` con el mismo `name` y el mismo string `YYYY-MM-DD` que el input nativo ya producía — cero cambios en schemas/Server Actions/servicios. Funciones puras en `src/lib/date-only.ts` (`maskUsDate`, `usDateToIso`, `isoToUsDate`).
- **Medicamentos y proveedores/médicos preferidos (Fase 019.8, Hallazgo #18):** nuevo `health-records.service.ts` sobre los modelos `PersonMedication`/`PersonProvider` ya existentes desde la migración 005 (sin cambios de schema). Viven en `Person`, nunca en `Policy` — se muestran siempre en la tab Salud del contacto, incluso sin ninguna póliza de Salud. "Eliminar" un medicamento marca `isActive=false` (nunca `DELETE` físico, preserva historial); "Eliminar" un proveedor sí es un `DELETE` físico (no tiene campo equivalente).

## Administración de Carrier/Product — Fase 012

- **`carriers.service.ts` y `products.service.ts`** siguen el mismo esqueleto que el resto de servicios: `actor` primero, `select` explícito, `AppError` para todo caso previsible. Crear/editar/activar-desactivar exige `actor.role === "ADMIN"` (`assertCanManageCarriers`/`assertCanManageProducts`); ver el catálogo está abierto a cualquier usuario activo.
- **`/settings` vive en su propio segmento de ruta**, no anidado bajo `/contacts` ni `/policies` — es administración transversal, no parte del flujo operativo diario. Las páginas de creación/edición (`/settings/carriers/new`, `/settings/products/[id]/edit`, etc.) verifican `actor.role === "ADMIN"` igual que `contacts/[id]/edit` verifica `canEditPerson` — si falla, renderizan un mensaje "no autorizado" en vez de redirigir, mismo patrón ya establecido.
- **Los botones "Activar"/"Desactivar" de las listas son un Client Component pequeño (`toggle-active-button.tsx`) con `useTransition` llamando directamente a una Server Action**, sin pasar por `useActionState`/`<form>` — es una mutación de un solo campo (`isActive`) sin campos que validar/mostrar en un formulario, así que el patrón más pesado de formulario no aporta nada aquí.
- **El formulario de edición de producto deshabilita (`disabled`) los campos bloqueados por uso histórico (`carrierId`/`policyType`/`planYear`) en vez de solo mostrarlos de forma read-only.** Un `<select>`/`<input>` `disabled` no viaja en el `FormData` al enviar — la UI ni siquiera intenta enviar un cambio que el servicio (`products.service.ts::updateProduct`) rechazaría de todas formas. La regla real sigue viviendo exclusivamente en el servicio; el `disabled` es conveniencia, no el límite de seguridad.
- **`policies.service.ts::assertActiveProduct`/`listActiveProducts` ahora también verifican `Product.carrier.isActive`, no solo `Product.isActive`** (cambio de esta fase) — un Carrier desactivado vuelve inelegibles todos sus productos para una póliza nueva sin tener que desactivarlos uno por uno. Las pólizas ya emitidas no se ven afectadas: la regla solo aplica al crear/cambiar de producto, nunca a lo ya guardado.
- **Bug preexistente encontrado durante las pruebas manuales, NO corregido en esta fase (fuera de alcance):** al abrir el menú de usuario (`UserMenu`, `src/components/shell/user-menu.tsx`, construido en la Fase 009) el navegador muestra un error de runtime — "Base UI: MenuGroupContext is missing. Menu group parts must be used within `<Menu.Group>` or `<Menu.RadioGroup>`" en `src/components/ui/dropdown-menu.tsx` (`DropdownMenuLabel`). No se tocó ningún archivo de esa fase en Fase 012; queda documentado para corregirse en una fase futura que toque el shell.
  **Corregido en Fase 013** (ver sección más abajo).

## Corrección de UserMenu — Fase 013

- **Causa raíz:** `DropdownMenuLabel` (`src/components/ui/dropdown-menu.tsx`) envuelve `MenuPrimitive.GroupLabel` de Base UI, que exige estar dentro de un `MenuPrimitive.Group` (expone `MenuGroupContext`). `UserMenu` usaba `<DropdownMenuLabel>` directamente como hijo de `<DropdownMenuContent>`, sin ese contenedor — de ahí el error de runtime "MenuGroupContext is missing" al abrir el menú.
- **Corrección:** se envolvió el `DropdownMenuLabel` en `<DropdownMenuGroup>` dentro de `user-menu.tsx` — el componente de la librería (`dropdown-menu.tsx`, generado por el preset shadcn `base-nova`) no se modificó. Es un error de uso, no un bug del preset: `DropdownMenuGroup` ya existía exportado y sin usar.
- **No se cambió de Base UI a Radix ni se reinstaló shadcn** — la corrección es el patrón correcto de composición que Base UI ya exige (`Group` envolviendo `GroupLabel`), simplemente no se había aplicado.
- Verificado manualmente: el menú abre, muestra nombre/rol, "Cerrar sesión" funciona, y la consola del navegador ya no reporta el error (confirmado con una recarga completa antes y después de abrir el menú, para descartar mensajes de consola acumulados de sesiones anteriores).

## Pólizas de salud (HealthPolicyDetail) — Fase 013

- **`health-policies.service.ts`** expone `getHealthPolicyDetail`, `createHealthPolicyDetail`, `updateHealthPolicyDetail` — sin `upsert` (ver docs/DECISIONS.md) y sin delete. Reutiliza `assertCanAccessPolicy` exportada desde `policies.service.ts` en vez de duplicar la política de acceso a `Policy`.
- **La redacción de ASSISTANT ocurre en el servicio, no en el componente.** `getHealthPolicyDetail` retorna un objeto que literalmente no tiene las claves `incomeUsed`/`taxCreditAmount` cuando `actor.role === "ASSISTANT"` (destructuring + omisión, no solo poner `null`) — el Server Component (`health-section.tsx`) solo decide si renderizar esas filas usando `"incomeUsed" in detail`, nunca decide qué ocultar por su cuenta.
- **`/policies/[id]/health` sirve tanto create como edit en una sola ruta**, decidiendo cuál Server Action usar (`createHealthDetailAction`/`updateHealthDetailAction`) según si `getHealthPolicyDetail` ya devolvió un registro — evita duplicar layout/estructura de formulario entre dos rutas casi idénticas.
- **El formulario (`health-detail-form.tsx`) recibe `showFinancialFields: boolean` y directamente omite la sección "Financiero Marketplace" del DOM para ASSISTANT** — no la deshabilita ni la oculta con CSS. Nada financiero llega al navegador de un ASSISTANT en primer lugar (ni en la carga de datos, por la redacción del servicio, ni en el formulario, por esta prop).
- **`form-helpers.ts` decide qué claves incluir en el objeto enviado al servicio usando `formData.has(campo)`, no `formData.get(campo)`.** Es la pieza que hace posible que un campo ausente del formulario (ASSISTANT sin `incomeUsed`) se traduzca en "no tocar" en vez de "borrar" — ver docs/DECISIONS.md para el razonamiento completo de los tres estados (ausente / vacío / con valor).

## Tareas (Task) — Fase 014

- **`tasks.service.ts`** expone `listTasks`, `getTaskById`, `getTasksForPerson`, `createTask`, `updateTask`, `completeTask`, `cancelTask` — sin delete. `completeTask`/`cancelTask` son atajos dedicados (transición de un solo campo) sobre la misma lógica de acceso que `updateTask`, pensados para el botón rápido de la UI; `updateTask` es el camino general de edición, con la regla adicional de que reabrir una tarea cerrada requiere ADMIN.
- **`isTaskOverdue` es una función pura exportada desde el servicio**, usada tanto en el `where` de `listTasks` (al construir el filtro `overdueOnly`/`dueToday`) como directamente en los componentes de UI (`/tasks`, la tab de Tareas de un contacto, la sección de una póliza) para pintar el badge "Vencida" — una sola definición de "vencida", nunca reimplementada en el cliente.
- **`/tasks/new` acepta `?personId=`/`?policyId=` opcionales** (desde "Nueva tarea" en el perfil de un contacto o el detalle de una póliza) y muestra el contexto ("Contacto: X" / "Póliza: Y — titular") en vez de un selector — no hay búsqueda interactiva de Person/Policy dentro del formulario de tareas, mismo principio de simplicidad ya aplicado al selector de titular en `/policies/new` (Fase 011).
- **El selector "Responsable" se omite completamente del formulario (creación y edición) cuando `actor.role === "AGENT"`**, no solo se deshabilita — un AGENT nunca ve la posibilidad de elegir a alguien más, coherente con que el servicio ignora/rechaza cualquier intento de todas formas.
- **Bug real encontrado y corregido en esta fase, no específico de Tareas:** ver "Fase 014" en `docs/DECISIONS.md` — un filtro de lista vacío enviado por `<form method="GET">` rompía `/contacts`, `/policies`, `/settings/products` y `/tasks` por igual. Corregido de forma centralizada en `src/schemas/common.ts` (`optionalSearchFilter`/`optionalUuidFilter`), aplicado en los cinco schemas de consulta afectados.

## Zona horaria de negocio — Fase 015

- **`src/lib/business-time.ts`** es el único punto de la aplicación que lee `APP_TIME_ZONE` y hace aritmética de calendario contra una zona horaria — `getAppTimeZone()` (validado y cacheado), `getBusinessDateParts()` (componentes Y/M/D de un instante en la zona de negocio), `startOfBusinessDay()`/`getTodayBusinessRange()` (límites de día como instantes UTC, para construir `where` de Prisma), y `formatInBusinessTimeZone()` (presentación). Ningún servicio debe calcular "hoy" por su cuenta con `new Date()` + `setHours(0,0,0,0)` — ese era exactamente el bug de Fase 014 que esta fase corrige.
- **`src/lib/date-only.ts`** resuelve un problema distinto: leer una columna `@db.Date` (`Person.dateOfBirth`) de forma segura, sin importar la zona horaria del proceso. No depende de `APP_TIME_ZONE` — usa getters UTC porque Prisma entrega esas columnas ancladas a medianoche UTC del día calendario real. `business-time.ts` (zona horaria de negocio, para "ahora") y `date-only.ts` (lectura segura de una fecha pura almacenada) son deliberadamente dos módulos separados con responsabilidades distintas, aunque el módulo de Cumpleaños use ambos juntos.
- **`isTaskOverdue` no usa ninguno de los dos módulos** — comparar `dueAt` (instante) contra `new Date()` (instante) no depende de zona horaria alguna. Documentado explícitamente en el código para que nadie intente "corregirlo" innecesariamente.

## Cumpleaños (Birthdays) — Fase 015

- **`birthdays.service.ts`** expone `listBirthdays`, `getBirthdayForPerson`, `markBirthdayGreetingSent`, `markBirthdayGreetingSkipped`, `resetBirthdayGreeting` — sin `create`/`update` genéricos porque no hace falta: marcar SENT/SKIPPED ya es la única forma de "escribir" en `BirthdayGreeting`, vía `upsert`.
- **`computeNextOccurrence` (cálculo de la próxima ocurrencia de un cumpleaños, incluyendo el cruce diciembre→enero) se exporta como función pura**, separada de cualquier consulta a Prisma — permite probar el cruce de año de forma determinista con fechas de referencia fijas, sin depender de la fecha real del sistema en el momento de correr los tests (mismo motivo por el que `isTaskOverdue` también es una función pura exportada, Fase 014).
- **`listBirthdays` calcula la vista (`today`/`month`/`upcoming`/`all`) completamente en memoria después de traer las `Person` con `dateOfBirth` no nulo** (filtradas server-side por acceso), en vez de intentar expresar "mes de nacimiento = mes actual" como una condición SQL — Prisma no tiene una forma declarativa de extraer mes/día de una columna `Date`, y el volumen de contactos de una sola agencia de seguros hace innecesaria esa complejidad. La restricción de acceso (AGENT limitado a su cartera) sí ocurre en el `where` de Prisma, antes de traer los datos a memoria — nunca se trae la lista completa para filtrar después en el servidor ni en el cliente.
- **El botón "Marcar felicitación" en `/contacts/[id]` reutiliza los mismos componentes de `/birthdays`** (`MarkSentDialog`, `SkipGreetingButton`, importados directamente desde `src/app/(app)/birthdays/`) — evita mantener dos implementaciones del mismo diálogo/confirmación en dos lugares del árbol de rutas.

## Comisiones — Fase 016

- **`commissions.service.ts`** expone `listCommissionExpectations`, `getCommissionExpectationById`, `getCommissionsForPolicy`, `createCommissionExpectation`, `updateCommissionExpectation`, `cancelCommissionExpectation`, `addCommissionPayment` — sin `updateCommissionPayment`/`deleteCommissionPayment` (ver docs/DECISIONS.md, política de no reescribir pagos).
- **`assertModuleAccess` rechaza ASSISTANT al inicio de cada función exportada del servicio** — a diferencia del resto de módulos (donde ASSISTANT suele equivaler a ADMIN para efectos administrativos), Comisiones es el primer módulo con un rol completamente excluido. `assertAdminOnly` restringe además toda mutación a ADMIN; AGENT solo pasa `assertModuleAccess` para lectura, acotada por `agentCommissionAccessWhere` (mismo patrón que `agentAccessWhere` en `policies.service.ts`).
- **`computeCommissionStatus` es una función pura y exportada** (mismo patrón que `isTaskOverdue`/`computeNextOccurrence`), usada tanto en el servicio (para adjuntar `derivedStatus` a cada expectativa devuelta) como probada exhaustivamente en `commissions.service.test.ts` sin depender de Prisma. `sumPayments` (también pura y exportada) centraliza la suma de movimientos usando `Prisma.Decimal`, nunca `Number`/`parseFloat`.
- **`normalizePaymentAmount` es la única función que decide el signo final de `CommissionPayment.amount`** — recibe el `type` y el monto "amigable" tal como lo escribió el usuario, y aplica la convención documentada en `docs/DECISIONS.md` (PAYMENT positivo, CHARGEBACK negado, ADJUSTMENT con signo preservado). Ni el schema de Zod ni el formulario deciden el signo — evita que dos lugares distintos puedan desincronizarse sobre la misma regla.
- **`experimental.authInterrupts` habilitado en `next.config.ts` + `src/app/forbidden.tsx`** — primera vez que la aplicación usa `forbidden()` (`next/navigation`) en vez de `redirect()`. Usado únicamente en `/commissions` y `/commissions/[id]` para dar a ASSISTANT un 403 real; el resto de la aplicación sigue usando `requireRole()` (redirect a `/dashboard`) donde eso es suficiente.
- **`nav-content.tsx` ahora recibe `role` como prop** (encadenado desde `AppLayout` → `Sidebar`/`Header` → `MobileNav`/`NavContent`) para poder omitir el ítem "Comisiones" del menú completo para ASSISTANT — antes ningún ítem de navegación dependía del rol del usuario.
- **`getCommissionsForPolicy` es una llamada explícita y separada, nunca parte del `select` de `getPolicyById`** — mismo patrón de minimización de datos que `HealthPolicyDetail` (Fase 013). La sección "Comisiones" de `/policies/[id]` (`commissions-section.tsx`) ni siquiera se renderiza (y por lo tanto nunca llama al servicio) cuando `actor.role === "ASSISTANT"`.
- **El punto de entrada para crear una expectativa es siempre `/policies/[id]` → "Agregar comisión esperada"**, nunca una lista abierta de pólizas en `/commissions/new` — la página redirige a `/commissions` si no llega `?policyId=`. Mismo principio de simplicidad ya aplicado al selector de titular en `/policies/new` (Fase 011) y al contexto de `/tasks/new` (Fase 014).

## Primas / Seguimiento de pago — Fase 017

- **`premiums.service.ts`** expone `listPremiumTracking`, `getPremiumTrackingForPolicy`, `updatePremiumTracking`, `markPaymentCurrent`, `markPaymentDue`, `markPaymentPastDue` — todas operan directamente sobre `Policy`, no existe ninguna entidad propia. Reutiliza `assertCanAccessPolicy` de `policies.service.ts` para autorización (a diferencia de `commissions.service.ts`, no rechaza a ASSISTANT).
- **`isPaymentOverdue` es una función pura y exportada** (mismo patrón que `isTaskOverdue`/`computeNextOccurrence`/`computeCommissionStatus`), probada con fechas de referencia fijas, incluyendo el cruce diciembre→enero. Las ventanas de fecha del listado (`dueToday`/`next7Days`/`next30Days`/`overdueOnly`) sí se filtran en SQL (`Prisma.PolicyWhereInput` con rangos sobre `nextPaymentDueDate`), a diferencia de Cumpleaños (Fase 015), porque aquí el rango no necesita ignorar el año — un simple `gte`/`lte` es suficiente y más eficiente que traer todo a memoria.
- **`nextPaymentDueDate` es `@db.Date`** — el formulario usa `<input type="date">` y el schema lo interpreta con `new Date(v)` (una cadena "YYYY-MM-DD" se parsea como medianoche UTC por la especificación ECMA-262 de fechas puras), sin necesidad de ninguna lógica de zona horaria — mismo principio que `effectiveDate`/`terminationDate` en `policy.schema.ts`. Para lectura se reutiliza `getDateOnlyParts` (`src/lib/date-only.ts`, Fase 015), nunca getters locales.
- **`updatePremiumTracking` construye su objeto `data` de Prisma explícitamente, campo por campo** (nunca `...input`) — es la garantía de código de que esta función no puede escribir ningún campo de `Policy` fuera de los 6 de seguimiento de pago, independientemente de lo que llegue en el `input` ya validado por Zod.
- **`markPaymentCurrent`/`markPaymentDue`/`markPaymentPastDue` son atajos delgados sobre un `setPaymentStatus` privado** que solo actualiza `paymentStatus` — nunca tocan `nextPaymentDueDate` (ver docs/DECISIONS.md).
- **La sección "Prima y pagos" de `/policies/[id]` (`premium-section.tsx`) hace su propia llamada a `getPremiumTrackingForPolicy`**, separada de `getPolicyById` — mismo patrón de secciones independientes ya usado por `HealthPolicySection`/`PolicyCommissionsSection`/`PolicyTasksSection`, aunque en este caso los 6 campos ya estaban disponibles en el `policySelect` básico; la llamada separada existe para reutilizar `isOverdue` (calculado por el servicio) sin duplicar esa lógica en el componente de página.
- **Botones de acción rápida (`QuickPaymentStatusButtons`) viven en `policies/[id]/quick-payment-status-buttons.tsx`**, un Client Component que importa las Server Actions desde `./premium/actions.ts` — mismo patrón que `TaskActionButtons`/`CancelExpectationButton` (`useTransition`, sin redirect, solo revalida).

## Dashboard — Fase 018

- **`dashboard.service.ts` es una capa de COMPOSICIÓN, no de negocio.** Expone una sola función pública, `getDashboard(actor)`, que orquesta llamadas a `tasks.service`, `premiums.service`, `birthdays.service`, `policies.service` y (condicionalmente) `commissions.service` — nunca construye su propio `where` de Prisma contra reglas de acceso o de vencimiento que ya existen en otro servicio. Internamente se organiza en 5 funciones privadas por bloque (`getTasksBlock`, `getPremiumsBlock`, `getBirthdaysBlock`, `getPoliciesBlock`, `getCommissionsBlock`), cada una responsable de un bloque del DTO.
- **El DTO (`DashboardData`, inferido con `Awaited<ReturnType<typeof getDashboard>>`) nunca incluye modelos Prisma completos.** Cada bloque proyecta explícitamente solo los campos que la UI necesita (ej. `priorityItems` de Tareas trae `id/title/priority/status/dueAt/isOverdue/personId/policyId`, nunca el objeto `Task` completo con `description`, timestamps de auditoría, etc.).
- **La clave `commissions` se agrega condicionalmente al objeto de retorno** (`if (actor.role === "ASSISTANT") return {...sin commissions}`), no se incluye como `null` — mismo patrón de omisión ya usado para redactar campos sensibles de `HealthPolicyDetail` (Fase 013).
- **`/dashboard` (Server Component) hace una única llamada a `getDashboard(actor)`** y renderiza todo con los datos ya resueltos — sin fetching en el cliente, sin polling, sin auto-refresh. Recargar la página trae datos frescos; las Server Actions relevantes ya llaman `revalidatePath("/dashboard")` (ver docs/DECISIONS.md para la lista exacta).
- **Los enlaces del Dashboard usan exclusivamente los query params reales que cada página ya interpreta** — `/tasks?dueToday=true`/`?overdueOnly=true` (no `?view=today`, que Tasks no soporta), `/premiums?overdueOnly=true`/`?dueToday=true`/`?next7Days=true`/`?needsAssistance=true`, `/birthdays` (usa `?view=today`/`?view=upcoming`, que sí soporta), `/policies?status=ACTIVE`/`?status=PENDING`, `/commissions` (sin filtro, el mes actual se filtra dentro de ese módulo si el usuario lo desea). Se verificaron los `SearchParams` reales de cada página antes de escribir cada `href`, en vez de asumir una convención uniforme entre módulos (Tasks y Premiums usan flags booleanos por filtro; Birthdays usa un único `view`; Commissions usa `period`).

## Importación de datos legacy — Fase 019

- **`src/import/` vive fuera del árbol de la app web** (`src/app`), no expone ninguna ruta HTTP ni Server Action — es una herramienta de línea de comandos (`scripts/import-legacy.ts`, `npm run import:legacy`) para uso administrativo puntual, nunca parte del flujo normal de la aplicación.
- **Ningún archivo de `src/import/` importa `"server-only"`** — a diferencia de `src/services/*.ts`, deliberadamente: este código debe poder ejecutarse desde un script `tsx` plano (sin el runtime de Next.js) tanto en producción (`npm run import:legacy`) como en tests de Vitest. La autorización de la app (`requireSessionUser`/actor) no aplica aquí — es una herramienta administrativa de confianza, no una ruta de usuario; escribe directo con Prisma, igual que `scripts/seed-dev.ts`.
- **`plan.ts` es la única función que lee el workbook.** Compone `clientes-sheet.ts` (Person/Household/Policy/HealthPolicyDetail) y `commissions-sheet.ts` (CommissionExpectation/CommissionPayment), nunca escribe en PostgreSQL — separación estricta entre "construir el plan" (`plan.ts`, puro respecto a la DB salvo lecturas de matching) y "escribir el plan" (`apply.ts`, la única función que hace `prisma.$transaction` con `create`).
- **`matching.ts::matchAgainstPool` es una función pura** reutilizada tanto para matching local (dentro del mismo run, vía `PersonRegistry`) como para matching contra PostgreSQL — una sola implementación de las reglas STRONG/MEDIUM/WEAK, nunca dos.
- **`workbook.ts::cellByHeader` lanza si se intenta leer una columna `EXCLUDED_SENSITIVE`** (`sensitive.ts`) — la exclusión de datos sensibles es una garantía de código en el único punto de lectura de celdas, no una convención que cada parser deba recordar aplicar.

## Correcciones funcionales, extensiones de modelo y UX — Fase 019.5

- **`FileStorage` (`src/lib/storage.ts`) es una interfaz (`save`/`read`/`delete`), no una integración concreta.** `LocalFileStorage` (dev) es la única implementación hoy; un adapter S3-compatible para producción se agrega implementando la misma interfaz, sin tocar `policy-documents.service.ts`. `generateStorageKey()` es la única función que produce un `storageKey` — siempre `crypto.randomUUID()` + extensión derivada del MIME ya verificado, nunca a partir del nombre de archivo del usuario.
- **`src/lib/file-sniff.ts::sniffMimeType` lee los primeros bytes reales del archivo** (firma binaria: `%PDF`, cabecera PNG, `0xFFD8FF` de JPEG, `RIFF....WEBP`) — es la única fuente de verdad del tipo de archivo en `uploadPolicyDocument`; el `Content-Type` que declara el navegador y la extensión del nombre de archivo nunca se usan para decidir si un archivo se acepta.
- **La descarga de documentos usa un Route Handler (`src/app/api/policies/[id]/documents/[documentId]/route.ts`), primera vez en el proyecto** — necesario porque streamear un binario con headers `Content-Type`/`Content-Disposition` no encaja en el modelo de Server Component/Server Action. Reutiliza `requireSessionUser()` y `assertCanAccessPolicy` exactamente igual que cualquier Server Action — no es una ruta con reglas de autorización distintas solo por ser un Route Handler.
- **`commission-rules.service.ts::resolveApplicableRule` implementa precedencia explícita, nunca combina reglas**: override de `Policy` (si existe y activo) gana sobre la regla de `Product` (si existe y activa); si no hay ninguna, `generateExpectationForPeriod` retorna `NO_RULE` en vez de generar con un default inventado.
- **`computeExpectedAmount` es una función pura y exportada** (mismo patrón que `isTaskOverdue`/`computeCommissionStatus`/`isPaymentOverdue`), sin llamadas a Prisma — permite probar determinísticamente cada combinación de método/base/periodicidad/residual sin depender de datos reales en la base.
- **`users.service.ts::createUser` y `scripts/create-admin.ts` construyen `User`+`Account` directamente con `better-auth/crypto::hashPassword`, en vez de llamar `auth.api.signUpEmail`.** Consecuencia directa de deshabilitar `emailAndPassword.disableSignUp` (ver docs/SECURITY.md): esa ruta de Better Auth queda bloqueada incondicionalmente, incluso para una llamada interna del propio servidor, así que la única forma de crear una cuenta con contraseña utilizable es reproducir manualmente lo que esa ruta hacía — mismo hash, misma convención de `Account.issuer`/`providerId` (`"local:credential"`/`"credential"`) ya establecida en la migración 007.
- **Los tokens de marca (`--brand-*`) son variables CSS planas en `globals.css`, remapeadas sobre las variables ya existentes de shadcn/ui (`--primary`, `--sidebar-*`, `--ring`, etc.), nunca hardcodeadas en componentes individuales.** Un componente nuevo que use `bg-primary`/`text-primary-foreground` hereda la marca automáticamente sin que nadie tenga que recordar el hex — cambiar el color de marca en el futuro es un cambio de un archivo.

## Auditoría, historial y búsqueda global — Fase 019.9

Detalle funcional completo del audit trail en [AUDIT_TRAIL.md](./AUDIT_TRAIL.md); aquí solo cómo se integra en la arquitectura.

- **`audit.service.ts` (`recordAuditEvent`/`buildDiff`) es el único punto de escritura de `AuditEvent`** — cada servicio de módulo llama a `recordAuditEvent` dentro de su propia transacción, nunca hay un `prisma.auditEvent.create(...)` disperso fuera de ese servicio central. `history.service.ts` es el lado de lectura (`getContactTimeline`/`getPolicyTimeline`/`getLastActivityForPerson`), completamente separado — ningún servicio de módulo depende de `history.service.ts`.
- **`src/components/history-timeline.tsx` es un Client Component compartido entre Contact Detail y Policy Detail** (`"Mostrar más"`, expandir "Ver cambios") — recibe `createdAtDisplay` ya formateado como string desde el servidor, nunca un `Date` crudo ni importa `business-time.ts` directamente: ese archivo depende de `"server-only"` (lee `APP_TIME_ZONE` de `process.env`), así que no puede evaluarse en el bundle de cliente. Los Server Actions de paginación (`history-actions.ts` en `contacts/[id]/` y `policies/[id]/`) formatean cada página antes de devolverla.
- **`loadMoreContactHistory`/`loadMorePolicyHistory` se pasan a `<HistoryTimeline>` vía `.bind(null, ...)`, nunca envueltos en una función local ad-hoc** — Next.js solo permite pasar de Server a Client Component una función que sea literalmente una Server Action (o el resultado de aplicarle `.bind`); un wrapper `async (cursor) => loadMoreX(id, cursor)` definido dentro del Server Component revienta en build ("Functions cannot be passed directly to Client Components"). Por eso `loadMoreContactHistory` recibe `cursor` como ÚLTIMO parámetro (no en medio) — así `.bind(null, personId, category)` deja exactamente `(cursor)` como firma restante, la que espera el componente cliente.
- **`search.service.ts::globalSearch` corre dos `findMany` independientes con `Promise.all` (nunca `$transaction([...])`)** — mismo principio ya establecido para el hallazgo de concurrencia de `pg` (Fase 019.6): ambas queries seleccionan relaciones, exactamente el patrón que dispara el warning real si se fijaran a una sola conexión.
- **`renewPolicy` es una función nueva y separada en `policies.service.ts`, no una variante de `createPolicy`** — ver docs/DECISIONS.md para el razonamiento. `PolicyForm` (`src/app/(app)/policies/policy-form.tsx`) gana `defaultValues`/`defaultCoveredMemberIds`/`submitLabel` opcionales para poder reutilizarse tanto en `/policies/new` como en `/policies/[id]/renew` sin duplicar el formulario.

## Pulido pre-producción y conciliación de comisiones — Fase 020

Detalle funcional de conciliación en [COMMISSION_RECONCILIATION.md](./COMMISSION_RECONCILIATION.md); aquí solo cómo se integra en la arquitectura.

- **`export.service.ts` reutiliza `policyAgentAccessWhere`/`agentCommissionAccessWhere` ya existentes** (exportados desde `policies.service.ts`/`commissions.service.ts` para este fin) — el CSV nunca ve más filas de las que el mismo usuario ya vería en la lista normal del módulo. `src/lib/csv.ts::toCsv` es un builder RFC4180 mínimo (escritura), distinto y no relacionado con `commission-statements/csv.ts::parseCsv` (lectura, para reportes de comisión). Los tres endpoints (`/api/export/{contacts,policies,commissions}`) son Route Handlers — mismo patrón que la descarga de documentos de póliza (Fase 019.5): un CSV con `Content-Disposition: attachment` no encaja en Server Component/Server Action.
- **`src/services/commission-statements/` es un módulo aislado, importado solo por `reconciliation.service.ts` y su UI (`/commissions/reconciliation`)** — ningún otro servicio depende de él, y él mismo no reimplementa nada de `commissions.service.ts` (reutiliza `CommissionExpectation`/`CommissionPayment` tal cual). El patrón adaptador (`CommissionStatementAdapter` → `NormalizedCommissionRow`) es deliberado para que agregar una agencia nueva sea implementar un archivo nuevo, nunca tocar `matcher.ts`/`reconciliation.service.ts`.
- **`USDateTimeInput` (`src/components/ui/us-datetime-input.tsx`) resuelve la deuda documentada en Fase 019.8** (`Task.dueAt` había quedado fuera de `USDateInput` por ser fecha+hora). `src/lib/us-datetime.ts` es puro/client-safe (sin `"server-only"`) por la misma razón que `date-only.ts`: se importa desde un Client Component. La conversión real a instante UTC (interpretando la hora de pared del usuario en `APP_TIME_ZONE`) vive en `tasks.service.ts::resolveDueAt`, no en el schema — `task.schema.ts` lo importan `task-form.tsx`/`edit-task-form.tsx` (Client Components) para sus constantes de enum, así que no puede arrastrar `business-time.ts`.
- **Causa raíz real (no solo documentada) de la advertencia residual de `pg` encontrada y corregida en esta fase** — ver docs/DECISIONS.md para el detalle completo. Regla reforzada: dentro de un `$transaction(async tx => ...)`, ningún `select`/`include` con más de una relación en un write, y ninguna consulta lanzada con `Promise.all` — ambas cosas fijan sub-consultas a la única conexión pinneada de la transacción.

## Identidad sensible del contacto y reportes operativos — Fase 021

Detalle funcional en [SENSITIVE_PII.md](./SENSITIVE_PII.md); aquí solo la integración arquitectónica.

- **`src/lib/pii-crypto.ts` es el ÚNICO módulo que cifra/descifra PII** — ningún servicio llama a `crypto.createCipheriv` directamente; todos pasan por `encryptPii`/`decryptPii`. `src/lib/sensitive-identity-format.ts` es puro (normalización/enmascarado, sin cifrado) — separado deliberadamente para poder probarse sin `PII_ENCRYPTION_KEY`.
- **`sensitive-identity.service.ts` es el único servicio que toca `PersonSensitiveIdentity`/`PersonImmigrationDocument`** — ningún otro servicio (incluido `people.service.ts`) lee o escribe estas tablas directamente, ni siquiera `reports.service.ts` (que solo selecciona `immigrationCategory`, nunca los campos `*Encrypted`).
- **`RevealableField` (`src/components/ui/revealable-field.tsx`) es el único componente de UI que maneja un valor "revelado"** — genérico, reutilizado para SSN, USCIS/A-Number y número de documento (uno por fila). El valor vive solo en `useState` local, nunca en un store global ni en `localStorage`.
- **Reporte de clientes (`reports.service.ts::listClientReport`) y su exportación CSV (`export.service.ts::exportClientReportCsv`) comparten el mismo `where` de Prisma** (`buildClientReportWhere`, exportado) — nunca dos implementaciones de filtro que puedan divergir entre la vista paginada y el CSV.
